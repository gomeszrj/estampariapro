const { createClient } = require('@supabase/supabase-js'); const fs = require('fs'); const env = fs.readFileSync('.env', 'utf8'); const sbUrl = env.match(/VITE_SUPABASE_URL=(.*)/)[1]; const sbKey = env.match(/VITE_SUPABASE_ANON_KEY=(.*)/)[1]; const supabase = createClient(sbUrl, sbKey); supabase.from('products').select('*').ilike('name', '%manga%').then(r => console.log(JSON.stringify(r.data, null, 2)))  
